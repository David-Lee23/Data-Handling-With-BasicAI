# --- START OF FILE .env.example ---

# Backend Configuration
# LOG_LEVEL=DEBUG # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
DATABASE_PATH=./data/application_database.db # Path to the SQLite database file
UPLOAD_FOLDER=./data/uploads # Temporary storage for uploaded files
MAX_CONTENT_LENGTH=16777216 # Max upload size in bytes (e.g., 16 * 1024 * 1024 = 16MB)

# NLP Engine Configuration
NLP_MODEL_NAME=t5-small # Hugging Face model for Text2SQL (e.g., t5-small, t5-base, google/flan-t5-base)
EMBEDDING_MODEL_NAME=all-MiniLM-L6-v2 # Sentence Transformer model (e.g., all-MiniLM-L6-v2, all-mpnet-base-v2)
# NLP_DEVICE=-1 # Device for NLP models: -1 for CPU, 0 for GPU 0, 1 for GPU 1, etc. (Requires CUDA setup for GPU)

# --- END OF FILE .env.example ---